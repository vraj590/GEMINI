---
trigger: always_on
---

This is a sleeper file that judges love.

Core rules:

JSON schemas are contracts, not suggestions.

Backward compatibility must be maintained for demo scenarios.

All stored artifacts must reference session_id and step_id.

No silent schema changes without updating fixtures.